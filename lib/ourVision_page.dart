import 'package:flutter/material.dart';

class OurVisionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Our Vision"),
      ),
      body: new Center(
        child: new Text("Our Vision"),
      ),
    );
  }
}
